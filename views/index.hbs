<div class="max-w-4xl w-full mx-auto p-4 md:p-6">
  {{#if homepage}}
  <section class="mt-8 mb-20">
    <div class="flex flex-col lg:flex-row items-center gap-16 lg:gap-20">

      <div class="lg:w-1/2 flex flex-col items-center lg:items-start text-center lg:text-left lg:pr-6">

        <div
          class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-primary/10 border border-primary/20 text-[11px] font-black uppercase tracking-[0.15em] text-primary mb-8 shadow-sm">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-primary"></span>
          </span>
          Developer Portfolio Showcase
        </div>

        <h1 class="text-4xl md:text-6xl font-black leading-[1.1] mb-8 tracking-tighter text-base-content">
          Share Ideas, <br class="hidden lg:block" />
          <span class="text-transparent bg-clip-text bg-linear-to-r from-green-500 to-purple-600">Showcase
            Talent.</span>
        </h1>

        <p class="text-lg opacity-80 leading-relaxed mb-12 max-w-md font-medium">
          A premium content platform engineered by <a
            class="text-base-content font-bold underline decoration-success/30" href="https://usman-ghani.dev"
            target="_blank">Usman Ghani</a> to demonstrate
          excellence in full-stack architecture, real-time interactions, and modern UI design.
        </p>

        <div class=" w-full max-w-sm flex flex-col gap-5">
          {{#if user}}
          <div class="flex flex-col items-center md:items-start gap-3">
            <a href="/your-feed"
              class="btn btn-success text-[1rem] h-14 btn-wide rounded-full dark:text-black md:text-lg shadow-lg shadow-success/20 transition-all hover:scale-[1.02] active:scale-95 flex items-center justify-center gap-2">
              <span data-feather="layout" class="h-5 w-5"></span>
              Enter My Feed
            </a>

            <a href="/posts/add"
              class="btn btn-primary text-[1rem] btn-outline border-primary  hover:bg-primary h-14 btn-wide rounded-full font-bold md:text-lg transition-all flex items-center justify-center gap-2">
              <span data-feather="plus-circle" class="h-5 w-5"></span>
              Create New Post
            </a>
          </div>

          <div class="flex flex-col items-center lg:items-start lg:pl-2">
            <p class="text-sm font-bold opacity-80 flex items-center gap-2">
              <img class="w-6 h-6 rounded-full" src="/assets/users/photo/{{user.username}}" alt="">
              Welcome back, <span class="text-base-content">{{user.username}}</span>
            </p>
          </div>

          {{else}}
          <a href="/users/oauth/google"
            class="btn btn-outline hover:bg-base-200 active:bg-base-300 dark:hover:bg-[#131314] flex items-center justify-center gap-4 w-full rounded-full border-base-300 h-14 transition-all shadow-sm hover:shadow-md">
            <svg version="1.1" class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48">
              <path fill="#EA4335"
                d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z">
              </path>
              <path fill="#4285F4"
                d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z">
              </path>
              <path fill="#FBBC05"
                d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z">
              </path>
              <path fill="#34A853"
                d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z">
              </path>
            </svg>
            <span class="text-base font-bold">Continue with Google</span>
          </a>

          <div class="flex flex-col items-center lg:items-start lg:pl-2">
            <p class="text-xs opacity-50 font-semibold tracking-wide uppercase">New here? Sign up is automatic.</p>
            <p class="text-[10px] opacity-40 mt-2">
              Review the <a class="link link-hover underline" href="/terms-of-services">Terms of Service</a>.
            </p>
          </div>
          {{/if}}
        </div>
      </div>

      <div class="lg:w-1/2 w-full relative mt-8 lg:mt-0">
        <div
          class="absolute -inset-4 bg-linear-to-r from-green-500/10 to-purple-500/10 rounded-[3rem] blur-3xl opacity-50">
        </div>

        <div class="relative bg-base-100 rounded-[2.5rem] shadow-2xl border border-base-300 overflow-hidden">
          <div class="bg-base-200/50 border-b border-base-300 px-8 py-8">
            <div class="flex items-center gap-3">
              <div class="p-2 bg-success rounded-xl">
                <span data-feather="terminal" class="h-5 w-5 text-success-content"></span>
              </div>
              <div>
                <h3 class="text-xl font-black tracking-tight leading-none">System Capabilities</h3>
                <p class="text-sm font-bold opacity-40 uppercase tracking-widest mt-1">Full-Stack Features</p>
              </div>
            </div>
          </div>

          <div class="p-8">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
              <div
                class="group p-4 rounded-2xl bg-base-200/30 border border-transparent hover:border-success/20 hover:bg-success/5 transition-all">
                <div class="p-2 bg-success/10 rounded-lg text-success w-fit mb-3">
                  <span data-feather="edit-3" class="h-4 w-4"></span>
                </div>
                <h4 class="text-sm font-black uppercase tracking-tight mb-1">Rich Editor</h4>
                <p class="text-sm opacity-80 leading-relaxed">Integrated Trix-editor for seamless content creation.</p>
              </div>

              <div
                class="group p-4 rounded-2xl bg-base-200/30 border border-transparent hover:border-purple-500/20 hover:bg-purple-500/5 transition-all">
                <div class="p-2 bg-purple-500/10 rounded-lg text-purple-500 w-fit mb-3">
                  <span data-feather="zap" class="h-4 w-4"></span>
                </div>
                <h4 class="text-sm font-black uppercase tracking-tight mb-1">Real-Time</h4>
                <p class="text-sm opacity-80 leading-relaxed">Live updates via WebSockets for comments and likes.</p>
              </div>

              <div
                class="group p-4 rounded-2xl bg-base-200/30 border border-transparent hover:border-blue-500/20 hover:bg-blue-500/5 transition-all">
                <div class="p-2 bg-blue-500/10 rounded-lg text-blue-500 w-fit mb-3">
                  <span data-feather="shield" class="h-4 w-4"></span>
                </div>
                <h4 class="text-sm font-black uppercase tracking-tight mb-1">Auth System</h4>
                <p class="text-sm opacity-80 leading-relaxed">Secure OAuth 2.0 Google integration and session handling.
                </p>
              </div>

              <div
                class="group p-4 rounded-2xl bg-base-200/30 border border-transparent hover:border-orange-500/20 hover:bg-orange-500/5 transition-all">
                <div class="p-2 bg-orange-500/10 rounded-lg text-orange-500 w-fit mb-3">
                  <span data-feather="database" class="h-4 w-4"></span>
                </div>
                <h4 class="text-sm font-black uppercase tracking-tight mb-1">Data Modeling</h4>
                <p class="text-sm opacity-80 leading-relaxed">Optimized Prisma schema for high-performance queries.</p>
              </div>
            </div>
          </div>
        </div>
      </div>

    </div>
  </section>
  {{else}}
  <div class="mb-8 pb-6 {{#unless feedPage}} px-2 {{/unless}} px-2 border-b border-base-300/50">
    <div class="flex flex-col md:flex-row md:items-end justify-between gap-6">
      {{#if feedPage}}
      <div
        class=" flex flex-col gap-4 sm:flex-row sm:items-center-safe   justify-between w-full  bg-base-100 border-base-300 rounded-2xl p-4 pb-0 px-0">
        <div>
          <h2 class="text-lg font-bold  uppercase tracking-widest mb-1">Your Feed</h2>
          <p class="text-sm opacity-80">Personalize topics for your reading experience.</p>
        </div>
        <div class=" flex flex-wrap gap-1">
          <div class="dropdown dropdown-start sm:dropdown-end">
            <div tabindex="0" role="button"
              class="btn btn-sm  btn-ghost hover:bg-success/30 border border-success/50 rounded-full normal-case font-medium px-5">
              Quick Select
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 opacity-80 ml-1" fill="none" viewBox="0 0 24 24"
                stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </div>
            <ul tabindex="0"
              class="dropdown-content z-1 menu p-2 max-h-[70vh] overflow-auto flex-nowrap  shadow-2xl bg-base-200 rounded-2xl w-52 mt-2 border border-success/20 backdrop-blur-md">
              <li class="menu-title text-[10px] uppercase tracking-widest">Categories</li>
              {{#each catgNameList}}
              <li><a class="rounded-xl py-2 hover:text-success" href="/explore/{{catgName}}">{{catgName}}</a></li>
              {{/each}}

              <div class="divider my-1 opacity-30"></div>
              <li><a class="rounded-xl py-2 opacity-90" href="/explore/All Posts">View All</a></li>
            </ul>
          </div>
          <button class="btn  btn-sm sm:mt-0 btn-outline btn-success opacity-95 w-max  rounded-full px-6"
            onclick="feed_modal.showModal()">
            Customize your feed
          </button>
        </div>

      </div>

      <dialog id="feed_modal" class="modal modal-bottom sm:modal-middle">
        <div class="modal-box p-0 shadow-2xl sm:max-w-xl">

          <div class="p-6 pb-4 border-b border-base-200">

            {{#if postlist}}
            <h3 class="font-bold text-xl tracking-tight">Customize Your Feed</h3>
            {{else}}
            <h3 class="font-bold text-xl tracking-tight">Your Current Feed is Empty</h3>
            {{/if}}
            <p class="text-sm opacity-70 mt-1">Select the categories you want to see.</p>
          </div>

          <form method="post" action="/users/profile/update/feed">
            <div class="max-h-[50vh] overflow-y-auto p-6 flex flex-wrap gap-2.5 bg-base-200/30 inset-shadow-sm">
              {{#each catgNameList}}
              <input class="btn rounded-2xl " {{#if checked}} checked {{/if}} type="checkbox" name="{{catgName}}"
                aria-label="{{catgName}}" />
              {{/each}}


            </div>

            <div class="p-4 border-t border-base-200 flex justify-end gap-3 bg-base-100 rounded-b-2xl">
              <button type="button" class="btn btn-ghost rounded-full" onclick="feed_modal.close()">Cancel</button>
              <input class="btn rounded-full" type="reset" value="Reset" />
              <button type="submit" class="btn btn-success rounded-full shadow-sm">Save Preferences</button>
            </div>

          </form>

        </div>

        <form method="dialog" class="modal-backdrop">
          <button>close</button>
        </form>
      </dialog>

      {{else}}
      <div class="space-y-1">
        <div class="flex items-center gap-2 text-sm uppercase tracking-wider text-success font-bold">
          <span>Exploring</span>
          <span class="opacity-30">/</span>
          <span>Category</span>
        </div>
        <h1 class="text-2xl md:text-4xl font-extrabold tracking-tight">
          {{catgName}}
        </h1>
      </div>

      <div class="flex flex-wrap items-center gap-3">

        <div class="dropdown dropdown-start sm:dropdown-end">
          <div tabindex="0" role="button"
            class="btn btn-sm btn-ghost hover:bg-success/30 border border-success/20 rounded-full normal-case font-medium px-5">
            Quick Select
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 opacity-80 ml-1" fill="none" viewBox="0 0 24 24"
              stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </div>
          <ul tabindex="0"
            class="dropdown-content z-1 menu p-2 max-h-[70vh] overflow-auto flex-nowrap  shadow-2xl bg-base-200 rounded-2xl w-52 mt-2 border border-success/20 backdrop-blur-md">
            <li class="menu-title text-[10px] uppercase tracking-widest">Categories</li>
            {{#each catgNameList}}
            <li><a class="rounded-xl py-2 hover:text-success" href="/explore/{{catgName}}">{{catgName}}</a></li>
            {{/each}}

            <div class="divider my-1 opacity-30"></div>
            <li><a class="rounded-xl py-2 opacity-90" href="/explore/All Posts">View All</a></li>
          </ul>
        </div>

        <div class="flex items-center gap-2 px-5 py-1.5 bg-success/10 border border-success/20 rounded-full">
          <span class="text-[10px] uppercase font-bold text-success ">Total</span>
          <span class="text-sm font-black text-success">{{total}}</span>
        </div>

      </div>
      {{/if}}
    </div>
  </div>
  {{/if}}

  {{#if postlist}}
  
  <h2 class=" text-sm font-bold opacity-70 uppercase px-2 tracking-widest mb-5">{{#if homepage}}Recent Posts
    {{else}}Latest Posts {{/if}} </h2>
  <div id="postlist" class="flex flex-col gap-4">
    {{#each postlist}}
    <div id="{{key}}"
      class=" group card bg-base-100 border transition-colors border-base-300 shadow-sm sm:shadow-none hover:shadow-md shadow-success/50  mb-4 overflow-hidden cursor-pointer">
      <div class="card-body p-5 md:p-7">

        <div class="flex items-start justify-between mb-4">
          <div class="flex items-center gap-3">
            <div class="avatar">
              <div class="w-10 h-10 rounded-full ring-2 ring-base-200">
                <img src="/assets/users/photo/{{auther.username}}" alt="{{auther.username}}">
              </div>
            </div>
            <div class="flex flex-col">
              <span class="font-black text-sm tracking-tight leading-none">{{auther.username}}</span>
              <span data-time="{{updatedAt}}"
                class="updatedAt text-[10px] font-bold uppercase tracking-widest opacity-40 mt-1"></span>
            </div>
          </div>

          <div id="{{key}}-info-container" class="flex flex-col items-end gap-1">
            <span id="{{key}}-info"
              class="badge badge-success badge-outline border-2 text-[10px] font-black uppercase px-2 py-3 rounded-lg hidden">

            </span>
          </div>
        </div>

        <div class="flex flex-wrap gap-2 mb-4">
          {{#each catgs}}
          <a href="/explore/{{catgName}}"
            class=" btn sm:btn-sm  btn-xs hover:btn-success p-0  px-2 sm:px-4 rounded-full ">
            #{{catgName}}
          </a>
          {{/each}}
        </div>

        <a id="{{key}}-link" href="/posts/view?key={{key}}" class="block ">
          <h2 id="{{key}}-title"
            class="title text-success sm:text-base-content text-2xl md:text-3xl font-black tracking-tight leading-[1.1] mb-3 group-hover:text-success transition-colors line-clamp-2">
            {{title}}
          </h2>
          <div id="{{key}}-body" class="text-base-content/90 text-sm md:text-base line-clamp-3 leading-relaxed">
            {{{body}}}
          </div>
        </a>

        <div class="card-actions justify-between items-center mt-6 pt-5 border-t border-base-200/60">
          <div class="flex items-center gap-1">
            <button data-postkey="{{key}}" class="like-btn btn btn-ghost btn-sm rounded-xl gap-2 text-success ">
              <span id="{{key}}-likeLogo" data-feather="thumbs-up" class="h-4 w-4"></span>
              <span id="{{key}}-likes" class="font-black text-xs">{{_count.likes}}</span>
            </button>

            <div class="btn btn-ghost btn-sm rounded-xl gap-2 text-info pointer-events-none opacity-80">
              <span data-feather="message-circle" class="h-4 w-4"></span>
              <span id="{{key}}-comments" class="font-black text-xs">{{_count.comments}}</span>
            </div>
          </div>

          <a href="/posts/view?key={{key}}" class="btn  btn-sm sm:btn-md btn-ghost p-0 px-2 text-base-content group-hover:text-success ">
           Read <span data-feather="arrow-right" class="h-5 w-9"></span>
          </a>
        </div>
      </div>
    </div>
    {{/each}}
  </div>

  {{#if homepage}}
  <div class="mt-16 mb-20 flex justify-center">
    <a class="btn btn-outline btn-wide rounded-full border-base-300 hover:bg-base-200 hover:text-base-content group"
      href="/explore/All%20Posts">
      Explore All Insights
      <span data-feather="chevron-right" class="h-4 w-4 transition-transform group-hover:translate-x-1"></span>
    </a>
  </div>
  {{/if}}
</div>

{{else}}
<div class="max-w-2xl mx-auto py-10 px-6 text-center">
  <div class="w-20 h-20 bg-base-200 rounded-3xl flex items-center justify-center mx-auto mb-8 border border-base-300">
    <span data-feather="edit-3" class="h-10 w-10 opacity-20"></span>
  </div>

  {{#if feedPage}}
  <h2 class="text-3xl font-black mb-4 tracking-tight">Your feed is quiet</h2>
  <p class="opacity-80 mb-10 max-w-sm mx-auto leading-relaxed">Follow more categories or users to see fresh content
    curated for you.</p>
  <div class="flex flex-col sm:flex-row gap-4 justify-center">
    <button class="btn btn-success rounded-full px-8 shadow-lg shadow-success/20"
      onclick="feed_modal.showModal()">Update Preferences</button>
    <a class="btn btn-ghost border border-base-300 rounded-full px-8" href="/explore/All%20Posts">Browse Global</a>
  </div>
  <script>document.getElementById("feed_modal").showModal()</script>
  {{else}}
  <h2 class="text-3xl font-black mb-4 tracking-tight">The base is empty</h2>
  <p class="opacity-80 mb-10 max-w-sm mx-auto leading-relaxed">Be the first one to share a technical insight or a
    personal thought with the world.</p>
  <a class="btn btn-success rounded-full px-10 shadow-lg shadow-success/20" href="/posts/add">
    Create Your First Post
  </a>
  {{/if}}
</div>
{{/if}}
</div>

<script>
  const postlist = document.getElementById('postlist');
  const emptyDiv = document.getElementById('emptydiv'); // Reference to hide this if a post comes in
  const ws = new WebSocket("{{wsURL}}")
  ws.addEventListener("message", (event) => {
    const data = JSON.parse(event.data);
    if (data.type === 'connection') {
      console.log(data.message);
    } else if (data.type === 'postcreated') {
      // 1. Hide empty div if it exists
      if (emptyDiv) emptyDiv.style.display = 'none';

      // 2. Create the wrapper
      const postDiv = document.createElement('div');
      postDiv.id = data.post.key;
      // Matches the CSS of the Handlebars loop above
      postDiv.className = "card bg-base-100 shadow-xl border border-base-300 transition-all hover:shadow-2xl";

      // 3. Construct HTML (Matches structure above)
      // post: Added safe checks for data.post properties
      const username = data.post.auther ? data.post.auther.username : 'User';


      postDiv.innerHTML = `
        <div class="card-body p-6">
          <div class="flex items-center justify-between mb-2">
            <div class="flex items-center gap-3">
              <div class="avatar placeholder">
                 <div class="">
                <img class="bg-purple-300 rounded-full dark:bg-neutral w-10 h-10" src="/assets/users/photo/${username}">
              </div>
              </div>
              <div class="flex flex-col">
                <span class="font-bold text-base">${username}</span>
                <span class="text-sm text-base-content/60">Just now</span> 
              </div>
            </div>
            <span id="${data.post.key}-info" class="badge badge-accent badge-sm">New</span>
          </div>

          <a id="${data.post.key}-link" href="/posts/view?key=${data.post.key}" class="group block space-y-2">
            <h2 id="${data.post.key}-title" class="card-title text-2xl group-hover:text-primary transition-colors">
              ${data.post.title}
            </h2>
            <p id="${data.post.key}-body" class="text-base-content/80 text-lg leading-relaxed">
              ${data.post.body}
            </p>
          </a>

          <div class="card-actions justify-between items-center mt-4 pt-4 border-t border-base-200">
             <div class="flex gap-4">
                <button class="btn btn-ghost btn-sm gap-2 text-success">
                  <span id="${data.post.key}-likes">0</span>
                  <span data-feather="thumbs-up" class=" h-4 w-4"></span>
                </button>
                <button class="btn btn-ghost btn-sm gap-2 text-info">
                  <span id="${data.post.key}-comments">0</span>
                  <span data-feather="message-circle" class=" h-4 w-4"></span>
                </button>
             </div>
          </div>
        </div>
      `;
      // Prepend to top of list (social feeds usually show newest first)
      postlist.prepend(postDiv);
    } else if (data.type === 'postdestroyed') {
      const postCard = document.getElementById(data.key);
      const postInfo = document.getElementById(data.key + "-info");
      const postLink = document.getElementById(data.key + "-link");

      if (postCard && postLink) {
        // Disable link
        postLink.onclick = (event) => event.preventDefault();
        postLink.removeAttribute("href");

        // Visual updates to look "Deleted"
        postCard.classList.add("opacity-50", "grayscale", "cursor-not-allowed");

        if (postInfo) {
          postInfo.textContent = "Deleted";
          postInfo.className = "badge badge-error badge-sm";
        }
      }
    } else if (data.type === "commentcreated") {
      const cSpan = document.getElementById(data.postkey + "-comments")
      cSpan.textContent = Number(cSpan.textContent) + 1;
      const infoSpan = document.getElementById(`${data.postkey}-info`)
      infoSpan.textContent = "New Comment"

    } else if (data.type === "commentdestroyed") {
      const cSpan = document.getElementById(data.postkey + "-comments")
      cSpan.textContent = Number(cSpan.textContent) - 1;
      const infoSpan = document.getElementById(`${data.postkey}-info`)
      infoSpan.textContent = "Comment deleted"

    } else if (data.type === "postupdated") {
      const infoSpan = document.getElementById(`${data.post.key}-info`)
      infoSpan.textContent = "Updated"
    }
  });

  class Times {
    constructor() {
      this.timesElements = Array.from(document.querySelectorAll(".updatedAt").values());
      this.dataTimesList = [];
    }
    main() {

      this.setRelativeTimes();
      const updateTimeInterval = setInterval(() => {
        this.setRelativeTimes(true)
      }, 1000 * 29)

      window.addEventListener("beforeunload", e => {
        clearInterval(updateTimeInterval)
      })
    }

    setRelativeTimes(interval) {
      this.timesElements.forEach(ele => {
        const dataTime = ele.getAttribute("data-time")
        if (!interval)
          this.dataTimesList.push(dataTime);
        ele.textContent = toRelativeTime(new Date(dataTime))
      })
    }
  }
  window.addEventListener("load", e => {
    new Times().main()
  })

  {{#if user }}
  class LikesControl {
    constructor() {
      this.likeButtons = document.querySelectorAll(".like-btn")
      console.log(this.likeButtons)
    }
    async init() {
      await this.setLikes()
      this.attachEvents()
    }
    async setLikes() {
      const res = await fetch('/users/likes/keys').then(async res => await res.text())
      if (!res) return
      const likeKeys = JSON.parse(res);
      Array.from(this.likeButtons).forEach(btn => {
        const postkey = btn.getAttribute("data-postkey")
        if (likeKeys.includes(postkey)) {
          btn.className = "like-btn btn btn-ghost btn-sm gap-2 text-primary";
          btn.setAttribute("data-isliked", "true");
          document.getElementById(postkey + "-likeLogo").setAttribute("class", "fill-primary h-4 w-4 ")
        }
      })

    }
    attachEvents() {
      Array.from(this.likeButtons).forEach((btn, i) => {
        btn.addEventListener("click", async (event) => {
          const postkey = btn.getAttribute("data-postkey")
          const userId = "{{user.id}}";

          if (btn.getAttribute("data-isliked") === "true") {
            const postkey = btn.getAttribute("data-postkey")
            const userId = "{{user.id}}";

            btn.disabled = true;
            btn.classList.replace("btn-ghost", "btn-active")
            try {
              const res = await this.fetchApi("/posts/likes/destroy", "POST", JSON.stringify({
                postkey: postkey,
                userId: userId
              }))
              if (res === "Ok") {
                this.toUnLiked(btn, postkey)
              }
            } catch (err) {
              console.error(err)
            } finally {
              setTimeout(() => {
                btn.disabled = false
              }, 500)
            }
          } else {
            const postkey = btn.getAttribute("data-postkey")
            const userId = "{{user.id}}";

            btn.classList.replace("btn-ghost", "btn-active")
            btn.disabled = true;
            try {
              const res = await this.fetchApi("/posts/likes/add", "POST", JSON.stringify({
                postkey: postkey,
                userId: userId
              }))
              if (res === "Ok") {
                this.toLiked(btn, postkey)
              }
            } catch (err) {
              console.error(err)
            } finally {
              setTimeout(() => {
                btn.disabled = false
              }, 500)
            }
          }
        })
      })
    }
    async fetchApi(url, method, body) {
      const res = await fetch(url, {
        body,
        method,
        headers: { "content-type": "application/json" }
      })
      return await res.text()
    }
    toLiked(btn, postkey) {
      btn.setAttribute("data-isliked", "true")
      btn.className = "like-btn btn btn-ghost btn-sm gap-2 text-primary";
      let totalLikes = document.getElementById(postkey + "-likes").textContent
      document.getElementById(postkey + "-likes").textContent = Number(totalLikes) + 1;
      document.getElementById(postkey + "-likeLogo").setAttribute("class", "h-4 w-4 fill-primary")
    }
    toUnLiked(btn, postkey) {
      btn.setAttribute("data-isliked", "false")
      btn.className = "like-btn btn btn-ghost btn-sm gap-2 text-success";
      document.getElementById(postkey + "-likes").textContent -= 1;
      document.getElementById(postkey + "-likeLogo").setAttribute("class", "h-4 w-4")

    }

  }
  window.addEventListener("load", () => {
    new LikesControl().init()
  })
  {{/if }}
</script>