<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User_Report_{{user.username}}</title>
    <style>
        /* PDF-like styling defaults */
        :root {
            --text-color: #1a1a1a;
            --muted-color: #555555;
            --border-color: #dddddd;
            --bg-color: #f0f0f0;
            --page-bg: #ffffff;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: "Segoe UI", "Helvetica", "Arial", sans-serif;
            margin: 0;
            padding: 20px 10px;
            line-height: 1.6;
        }

        /* The "A4" Page Container */
        .page {
            max-width: 800px;
            margin: 0 auto;
            background: var(--page-bg);
            padding: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            border: 1px solid #ccc;
        }

        /* Typography */
        h1, h2, h3 { 
            margin-top: 0; 
            border-bottom: 2px solid var(--text-color);
            padding-bottom: 5px;
        }
        
        .report-header { margin-bottom: 40px; }
        .report-header p { margin: 5px 0; font-size: 0.95rem; }
        
        /* Data Tables/Lists */
        .section-title {
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 1.1rem;
            margin-top: 30px;
            color: var(--muted-color);
            color: rgb(3, 16, 3);
            border-bottom: 1px solid var(--border-color);
        }

        .record {
            margin: 20px 0;
            padding-bottom: 15px;
            background:
            border-radius: 10px;
            border-bottom: 1px dashed var(--border-color);
        }

        .record-title {
            font-weight: bold;
            font-size: 1.1rem;
            color: green;
            display: block;
        }

        .metadata {
            font-size: 0.8rem;
            color: var(--muted-color);
            margin-bottom: 8px;
        }

        .body-text {
            font-size: 1rem;
            white-space: pre-wrap; /* Preserves line breaks in posts/comments */
        }

        .label {
            font-weight: bold;
            color: #333;
        }

        /* Print optimization */
        @media print {
            body { background: none; padding: 0; }
            .page { box-shadow: none; border: none; width: 100%; max-width: none; }
        }
    </style>
</head>
<body>

{{#if user}}
    <div class="page">
        <div class="report-header">
            <h1>Complete User Profile Data</h1>
            <p><span class="label">Display Name:</span> {{user.displayName}}</p>
            <p><span class="label">Username:</span> @{{user.username}}</p>
            <p><span class="label">Identity:</span> {{user.firstName}} {{user.lastName}} (via {{user.provider}})</p>
            {{#if user.about}}
                <p style="margin-top: 15px;"><span class="label">About:</span> {{user.about}}</p>
            {{/if}}
        </div>

        <h2 class="section-title">Authored Posts ({{user.posts.length}})</h2>
        {{#each user.posts}}
            <div class="record">
                <span class="record-title">
                    {{#if title}}{{title}}{{else}}Untitled Record{{/if}}
                </span>
                <div class="metadata">
                    Key: {{key}} | 
                    Status: {{#if public}}Public{{else}}Private{{/if}} | 
                    Likes: {{#if likes}}{{likes}}{{else}}0{{/if}}
                </div>
                <div class="body-text">{{{body}}}</div>
            </div>
        {{else}}
            <p class="metadata">No posts recorded.</p>
        {{/each}}

        <h2 class="section-title">Recent Comments & Activity</h2>
        {{#each user.comments}}
            <div class="record">
                <div class="metadata">
                    Comment ID: {{id}} | 
                    In response to post: <strong>{{#if post.title}}{{post.title}}{{else}}{{postkey}}{{/if}}</strong>
                </div>
                <div style="color: rgb(0, 28, 0);" class="body-text">{{body}}</div>
            </div>
        {{else}}
            <p class="metadata">No comment history available.</p>
        {{/each}}

        <div style="margin-top: 50px; text-align: center; font-size: 0.7rem; color: #aaa; border-top: 1px solid #eee; padding-top: 10px;">
            Generated on {{#if user.photo_updatedAt}}{{user.photo_updatedAt}}{{else}}2026{{/if}} | User Database System
        </div>
    </div>
{{else}}
    <div class="page" style="text-align: center;">
        <h1>Error</h1>
        <p>No user data was provided for this report.</p>
    </div>
{{/if}}

</body>
</html>